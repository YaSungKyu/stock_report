# gapbet 일일 리뷰 — 2026-09-11 (금)

## 0. 개요

* **검증일/시각**: 2026-09-11(금) 09:30 이후 KST (창 08:00/09:00~09:30 종료 후)
* **평가한 리포트**: **없음 — 결함.** 직전 거래일 2026-09-10(목)의 **postclose(A 모드) 리포트가 생성되지 않았다.**
* **원인(확인됨)**: `reports/logs/gapbet_2026-09-10.log` — 15:40 스캔이 3회 전부 같은 메시지로 실패.
  `You've hit your monthly spend limit ... your weekly limit resets 10pm (Asia/Seoul)` → 15:40 / 15:45 / 15:51 시도 후 `FAILED: no report after 3 attempts`.
  같은 날 14:45 **preclose(B 모드)도 같은 메시지로 실패**했다(`gapbet_preclose_2026-09-10.log`). 두 모드가 다 죽어 **B 바스켓 대체 경로도 없다.**
* **09-04와 다른 점**: 09-04는 리셋 시각이 **18:00**(NXT 애프터마켓 마감 20:00 안)이었고, 이번은 **22:00 — NXT 마감 뒤**다. 재시도를 20:00까지 늘려도(`pending_changes.md` ⓛ) 이번 회차는 못 살린다. 한도 증액은 **사용자만 할 수 있다**(메시지 자체가 `raise it at claude.ai/settings/usage`를 가리킨다) → §3·§4-8.
* **결과**: `gapbet_report_2026-09-10.md`·`scores_2026-09-10.jsonl` 없음 → **STEP 2~4(예측 읽기·판정·적재) 건너뜀.** `dataset.jsonl`·`gate_failed.jsonl` 신규 0행. **STEP 5(튜닝)는 정상 수행**(gapbet_review.md STEP 1).
* **실패 5번째 · 현금 슬롯 4개째**: 08-05 Ctrl+C(→08-06) · 08-14 API 529(→08-18) · 08-18 OAuth 만료 · 09-04 월 한도(→09-07) · **09-10 월 한도(→09-11)**. **최근 6거래일 중 2회가 사용 한도로 죽었다.**
* **오늘 아침 시장**: KODEX200 시초 **−3.61%** · 09:30 −2.71%, KODEX코스닥150 시초 **−3.42%** · 09:30 −2.92% — 전면 갭다운. 바스켓이 없으므로 이 슬롯의 기회비용은 **측정 불가**이고, 「현금이라 다행」 같은 해석은 쓰지 않는다(바스켓이 지정된 적이 없다).
* **집계 시점 주의**: `pnl.missing_trading_days`는 오늘 [08-06, 08-18, 09-07]이고 **09-11은 아직 안 들어 있다.** 구간이 `dates[-1]`(=09-10)에서 끝나기 때문이며, 다음 회차가 적재되면 자동 반영된다. **그때 북극성·`as_traded`가 한 칸 내려가는 것은 성적 악화가 아니라 이 슬롯이 늦게 잡히는 것이다.**
* **라벨-집행 일치**: `barcache.py prune`(944행, 버릴 것 없음) → `build`(944행) → `replay.py --verify` **rows 944, changed 0, uncached 0**. 불일치 0.
* **데이터 한계**: 새 표본 0. 아래 누적 수치는 전부 재실행한 값이며 09-10 리뷰와 일치한다.

## 1. Top 후보 성적표

**해당 없음 — 판정할 예측이 없다.** 09-10 postclose 리포트가 없으므로 매수 종목이 지시된 적이 없고, 오늘 회차는 **현금 슬롯**이다.

* 09-07처럼 B 모드 바스켓을 진단용으로 재현하는 것도 불가하다 — 09-10 preclose 리포트가 없다.
* 누적(북극성, `pnl.north_star`, 2026-08-21 이후 **14회차**): 회차복리 **+0.0177%** · 누적 +0.248%. **표본 부족(26회차 더 필요) — 246회차 배수 환산 금지.** 오늘 슬롯은 다음 회차 적재 때 분모에 들어간다.

## 2. 탈락 종목 검증

**해당 없음** — 채점된 모집단(`gate_passed` 양쪽) 자체가 생성되지 않았다. 게이트 탈락 표(2-1)·순위 미달 표(2-2) 모두 오늘 신규 행 0.

## 3. 빗나간 예측 분석

**해당 없음(예측 부재).** 실패는 모델이 아니라 **운영 경로**에서 났다.

| 실패 회차 | 원인 | 리셋 시각 | 현행 재시도(15:40~15:50) | ⓛ 확장 재시도(~19:30) | B 바스켓 대체 |
|---|---|---|---|---|---|
| 08-14 | API 529 | — | **복구 가능** | 가능 | — |
| 08-18 | OAuth 만료 | — | 불가 | 사람 개입 시 가능 | — |
| 09-04 | 월 한도 | **18:00** | 불가 | **복구 가능** | 가능(preclose 있음, +1.31%) |
| **09-10** | **월 한도** | **22:00** | 불가 | **불가 — NXT 마감(20:00) 뒤** | **불가 — preclose도 실패** |

* **09-10 유형은 스킬 안의 어떤 재시도·대체 경로로도 못 막는다.** 남은 경로는 한도 자체(사용자 계정 설정)뿐이고, 첫 실패(15:40) 시점에 NXT 진입 창이 **4시간 20분** 남아 있었다. 사용자가 그 안에 알았다면 한도를 올리고 재시도로 받을 수 있었다 — **실패가 조용한 것이 두 번째 원인**이다(08-21(8)이 이미 지적한 성질이 여기서 다시 비용이 됐다).

## 4. 누적 집계 & 오늘의 제안

### 4-1. 성적 — 네 줄을 합치지 않는다

| 성적의 종류 | 값 |
|---|---|
| **북극성 — 답을 안 보고 매 회차 다시 뽑아 산 결과**(`sweep.py --walk`, 23회차) | **회차당 +0.3016% · 누적 +7.17% · 손실 12회차**<br>t = +0.70 (n=23, 표준편차 2.24%p) → **0과 구분 불가**<br>**추세: 전반 11회차 +0.5649%/회차 → 후반 12회차 +0.0609%/회차** |
| 현행 규칙으로 실제 산 것 (`pnl.north_star`, 2026-08-21 이후 14회차) | 회차복리 **+0.0177%** · 누적 +0.248% · **표본 부족(26회차 더 필요), 배수 환산 금지** |
| 지금 배점을 과거 전부에 대입 (답을 보고 푼 값, `sweep.py --pick`) | LOO 최악 **+0.3279%** · 전체 +0.5156% · 최악회차 −2.72% · 손실 16/33회차 |
| 시작부터 지금까지 실제 산 것 전부 (규칙 섞임, `pnl.as_traded`) | 회차복리 +0.1145% · 누적 +4.33%(37슬롯, 09-11 미포함) — **킬스위치 전용, 근거 인용 금지** |

* **수익률 축**: 「그날 답을 보고 맞춘 값 vs 그날 실현」 격차 전반 **−0.1512%p** → 후반 **−0.5404%p**. 표본내 적합값은 +0.7161 → +0.6013으로 유지되고 실현만 빠졌다 — 해석은 과적합 쪽이지만 전반−후반 차 +0.5284%p의 **Welch t = +0.54로 국면 변화와 구분되지 않는다.**
* **배점 궤적 축**: 직전 회차 대비 L1 이동 평균 14.5(전반 19.1 → 후반 10.0), 최종 배점과의 거리 70 → 0, **최종 배점 연속 유지 4회차**. 표본이 안 늘어 09-10과 같다.
* 각주: `--walk`가 재적합하는 것은 **배점뿐**이다. 게이트·집행 규칙 세대(08-05 갈래 통합, 08-10 상위 3종목 고정, 08-21 3주체 전환)는 dataset 행에 굳어 있다.

### 4-2. 손익 구조 (`pnl`)

* n=103행 · 평균 +0.143% · 중앙값 +0.48% · 표준편차 2.65%p · 양수율 0.524 · 최악 −6.69% · 최고 +8.18%
* **`negative_expectancy` = false** — 대원칙 8은 발동하지 않는다.
* **1차 조종간 `deep_loss_rate` = 0.068** · 깊은 갭하락률 0.078(불변)
* 청산 경로: trail 43 · stop 37 · time 19 · weak_dn_close 4
* 회차 단위: 34회차 중 손실 19회차(55.9%) · 최악 회차 −3.683%

### 4-3. 진단 지표 (조종간 아님)

* `gap_dir.separation_within_day` +0.019 · `mantel_haenszel_or` 1.008 · `day_variance_share` 0.219
* `gap_down_big_recovery`: 2/46 = **4.3% < 손익분기 6.0%** → **트레일 유지**
* `by_market_cap`: 5조+ 승률 0.558(n=86) · 1~5조 0.357(n=14) · 3천~1조 0.000(n=1)
* 회차 내 항목 판별력(`factor_separation_within_day`, 0.5=무신호): 재료 **0.667**(33) · 미장 0.636(22) · 섹터 0.556(45) · 종가 0.500(44) · 수급 0.468(47) · 과열 0.432(37) · 총점 0.600(50)
* `confound_warning` = true(neg 4 / pos 2) → `tuning_guard` **BLOCKED** — 항목 분리도로 배점을 옮기는 경로만 막는다. `sweep.py --pick` 경로는 대상 아님.

### 4-4. Type II 비용 (대원칙 9 — 비용은 이 둘뿐)

| 항목 | 값 |
|---|---|
| ⓐ 게이트가 놓친 상승 `gate_wrong_forgone_sum_pct` | **+241.24%p** 누적(51건, 평균 초과 +4.32%p). 오늘 신규 0 |
| ⓑ 트레일 조기 청산 포기분 `trail_giveback_mean_pct` | **2.248%p**/건 |

* 사유별 베타 조정 초과(`n_unbucketed` = **0** 정상): 장후 공시 **+1.74%p**(n=15) · 양측 순매도/개인 주도 +0.47%p(n=98) · 기관 단독 매수 −0.11%p(n=198) · 외국인 단독 매수 −0.11%p(n=239) · 수급 외 사유 +0.02%p(n=7) · 기타 수급미달 +0.92%p(n=1)
* 게이트 부가가치 `gate_rejected.selection_edge_pct` = **+0.417%p**(재설계 발동선 +0.3%p 위)
* 참고: 오늘 잃은 회차는 위 두 비용 어디에도 안 잡힌다 — 「안 바꿔서 잃은 것」이 아니라 **「돌지 못해서 잃은 것」**이고, 그 크기는 바스켓이 없어 측정되지 않는다.

### 4-5. 게이트 양방향 성적표 (`gate_scorecard`)

| | n | 평균 | 음수 | 깊은손실(≤−3%) |
|---|--:|--:|--:|--:|
| 통과 | 401 | +0.027% | 49.4% | **5.7%**(`leak_rate`, 23건) |
| 탈락 | 543 | −0.274% | 55.2% | 7.9%(43건) |
| | | | `neg_edge_pp` **+5.8%p** | `tail_edge_pp` **+2.2%p** |

* 회차 페어링 **11승 8패**. 조건선(+2%p) 위이나 진동 중이라 「유지」 확정 유보(표본 불변).
* `leak_rows` 23건 중 미매수분(`rank_scored`)은 배점의 공로로 쓰지 않는다.

### 4-6. 배점 서열 진단 (비용 아님)

* `session_edge_t` = **+0.69**(n=30, 평균 +0.218%p, 표준오차 0.315) → **신호 없음(0과 구분 불가). 적자가 아니다.**
* 통과 풀 `within_day_rank_tau` +0.036(n=28, 양수 14) · **`bought_only_tau` −0.025**(n=30)
* 무작위 3종목 추출 구간 중앙값 −0.0897%, [p05 −0.3992, p95 +0.2241]
* 참고: 4위 이하 미매수분 n=298 · 교체효과 평균 −0.131%p · 중앙값 −0.09%p

### 4-7. 미결 항목 판정 — **마감일 경과 0건.** 표본 불변이라 대부분 09-10과 같고, 새로 계산한 것만 적는다

| 등록일 | 항목 | 오늘 판정 |
|---|---|---|
| 07-30 | 「기관 단독 매수」 게이트 완화 | **조건 재유도 수행(09-10 지시분).** 「바뀌는 회차 0」이 ①수급 40 탓인지 확인하려고 **A 블록 배점(수급 15)에서도** 편입 반사실을 돌렸다 → **edge −0.0510%p · LOO 최악 −0.0527 · 바뀌는 회차 2.** 수급 배점을 내려도 게이트가 연 자리는 거의 집행에 닿지 않고, 닿는 두 회차는 손해다. **재유도 조건: 「적용 배점과 A 블록 배점 둘 다에서 편입 edge +0.05%p 이상·LOO 부호 유지·바뀌는 회차 n≥5」** — 현재 양쪽 미달. 마감 09-30(약 11거래일, 추석 휴장 제외) |
| 08-13 | `foreign_only` 게이트 완화 | 같은 재계산: 현행 배점 edge +0.0000(0회차) / **A 블록 배점 edge +0.0027%p · LOO 최악 −0.0274 · 바뀌는 회차 2** → 부호 유지 실패. `both_sell`은 A 블록에서도 −0.0436%p(1회차) |
| 08-05 | 채점 로직 부가가치 | ⓐ +0.417%p / ⓑ t=+0.69 → 「신호 없음」 유지, 역방향 아님 |
| 08-03 | 회차 스킵 탐색 종료 | 격차 4.04배(불변, 09-11 슬롯 미반영). 조건 1.5배 미달 |
| 08-03 | LOO → 시계열 holdout | `--pick` 33회차 약 16초 — 조건(3분) 미달 |
| 08-14 | 게이트가 왼쪽 꼬리를 자르는가 | `tail_edge_pp` +2.2%p(불변) — 관찰 계속 |
| 08-19 | 약세 국면 비중 축소 | 표본 불변 — 스킵 edge −0.1382%p · LOO −0.2604(09-10 기준) |
| 08-21 | 「장후 공시」 면제 확대 | +1.74%p(n=15), n 요건까지 5건(불변) |
| 08-21 | 품질 하한 게이트 | ④종가 θ0.2 선두(바뀌는 회차 2, 요건 10) — 불변 |
| 08-21 | 3주체 게이트 / 종목 집중 / 예외 존치 | 표본 불변 — +5.1%p / +9.6%p / 존치 유지(09-10 기준) |
| 08-21 | ②재료 배점 부활 | `--pick` ②=25 불변. A 블록과 같은 사안. 맞대결 누적 합 +0.21%p(판정 2·동률 2, 오늘 추가 없음) |
| 08-27 | 국면 문구 사문화 정리 | 표본 불변 — 네 문구 전부 스킵 손해 |
| 08-28 | 「강세·주도테마 지속」 회차 스킵 | 4/33회차, edge +0.1403 · LOO +0.0855(불변). n 요건 10 미달 |
| 09-01 | 애프터 프리미엄 상한 | 불변 — LOO 부호 유지 실패 |
| 09-02 | 종목별 과거 갭 변동성 상한 | 불변 — 실적으로 미달 |
| **09-07** | **A 모드 실패 회차에 B 모드 바스켓 보유** | **오늘 새 실패 회차가 생겼으나 발동 불가** — 09-10 preclose도 같은 한도로 죽었다. 잃은 슬롯 4개 중 preclose가 있던 것 2개(08-18 +3.197% · 09-07 +1.31%). **n=2 불변.** 두 모드가 같은 계정 한도를 공유하므로, **한도형 실패에서는 이 대체 경로가 구조적으로 같이 죽는다**는 사실을 조건 옆에 적어 둔다 |

### 4-8. 오늘의 제안

**A. 배점 — `sweep.py --pick` 기계 산출(매 회차 덮어쓰기)**

* **before → after**: `수급40 재료5 섹터5 종가0 미장20 과열30` → **`수급15 재료25 섹터40 종가0 미장0 과열20`** (LOO 최악 +0.3279% · 전체 +0.5156% vs 현행 +0.2808% · +0.4698%, 현행 6위)
* 표본이 안 늘어 **산출 불변, L1 이동 0.** 30%p 경보 없음. 오늘은 새 증거가 아니다. 좁혀진 후보는 여전히 섹터 축 vs 수급 축 **동률 군집**(격차 0.047%p).
* **채택은 사용자가 한다.** 적용 시 `analyze.py RULES_APPLIED_DATE`를 적용일로 옮긴다.

**B. 그 외 레버 — 기존 ⓛ 갱신 1건(신규 0)**

* **ⓛ 운영 경로 갱신**: 재시도 창 확장에 **「첫 실패가 사용 한도면 즉시 사용자 알림」**을 묶는다. 근거는 §3 표 — 09-10은 리셋이 NXT 마감 뒤라 재시도만으로는 못 받고, 한도는 사용자만 올릴 수 있으며, 첫 실패 시점에 진입 창이 4시간 20분 남아 있었다. 알림이 있으면 사용자가 올린 한도를 확장된 재시도(18:10·19:10)가 받는다. 통계 적합이 아니라 로그의 기계적 사실이 근거라 대원칙 4의 표본 게이트 대상이 아니다.
* 나머지 레버를 안 올리는 이유:

| 레버 | 오늘 데이터 | 왜 제안 안 하는가 |
|---|---|---|
| 게이트(완화) | A 블록 배점에서도 편입 edge −0.0510 / +0.0027(LOO −0.0274) / −0.0436%p | 어느 배점에서도 집행에 닿지 않거나 손해 |
| 게이트(강화) | `tail_edge_pp` +2.2%p | 발동선(0 근방·음수) 아님 |
| 회차 스킵 | §4-9 후보 2개 전 임계 음수 | 기각 |
| 집행 | `gap_down_big_recovery` 4.3% < 6.0% | 트레일 유지 |
| 국면 | 「강세·주도테마 지속」만 부호 유지(n=4) | n 요건 10 미달 |
| 배점 | A 블록 | 기계 산출 그대로 대기 |

* `pnl.negative_expectancy` = false → 대원칙 8 비발동.

### 4-9. 반사실 발굴 (대원칙 10 — 오늘 신규 2건, 전부 기각)

과거 리뷰에 계산 이력이 없는 **회차 스킵** 규칙 2개를 만들어 현행 배점 재구성(`40/5/5/0/20/30`, 상위 3종목) 기준선에 소급했다. 둘 다 스캔 시점에 이미 아는 값만 쓴다 → 누설 없음. 채점 풀이 온전히 기록된 **08-03 이후 25회차**에서만 계산했다(그 전은 매수분만 채점이 남아 풀 크기가 기록 결손이다).

| 후보 | 쓰는 값 | 결과 | 판정 |
|---|---|---|---|
| **게이트 통과율 스킵** — 그 회차 채점 후보 중 게이트 통과 비율이 θ 이상(또는 미만)이면 현금 | 통과 수 ÷ (통과 + 게이트 탈락) — 스캔이 STEP 4에서 확정 | 통과율 평균 0.437(0.162~1.000), 회차 수익과 상관 **r=+0.185**. 「θ 이상 스킵」 θ0.4~0.8: edge **−0.4088 ~ −0.1706%p**, 「θ 미만 스킵」: **−0.1561 ~ −0.3943%p**. LOO 최악 전부 음수(바뀌는 회차 1~24) | **기각** — 양방향 전 임계 음수. 통과율이 높은 날(동반매수가 넓게 퍼진 날)이 오히려 약간 나았다 |
| **상위 3종목 총점 우위 스킵** — 매수 3종목 평균 총점 − 풀 중앙값이 θ 미만(또는 이상)이면 현금 | 그 회차 총점 분포 | 우위 평균 12.88점(5분위 8.89/11.72/14.82/16.51), 상관 **r=−0.063**. 「우위 작으면 스킵」: **−0.2141 ~ −0.4277%p**, 「우위 크면 스킵」: **−0.1372 ~ −0.3509%p**. LOO 최악 전부 음수(바뀌는 회차 5~20) | **기각** — 총점이 풀에서 도드라지는지는 회차 수익과 무관(r≈0). 08-17 「총점 절대 하한」 기각과 같은 결론을 상대값으로도 확인 |

* 두 후보 모두 표본 부족이 아니라 실적으로 기각됐다. 같은 후보를 다시 계산하지 않기 위해 여기 남긴다.

### 4-10. 대기열 현황

* **미적용 제안 2건** — A(배점, 09-03 최초 산출 후 매 회차 동일값 덮어쓰기) · B ⓛ(**2026-09-07 등록, 4거래일 경과**, 오늘 갱신)
* 10거래일을 넘긴 B 항목 없음.
* **gapbet.md·kis_scan.py는 오늘 변경 0.** 다음 회차 기준선·라벨은 계속 gapbet.md 현재 값(`수급40 재료5 섹터5 종가0 미장20 과열30`)이다 → [`pending_changes.md`](../../.claude/skills/gapbet_review/pending_changes.md)
* `tuning_log.md` 롤링: 10거래일을 넘긴 2026-08-21(2)~(8) 엔트리를 `tuning_log/2026-08.md`로 원문 그대로 이관했다 → [`tuning_log.md`](../../.claude/skills/gapbet_review/tuning_log.md)

## Sources

* 판정 대상 리포트 **부재**(원인: `reports/logs/gapbet_2026-09-10.log`·`gapbet_preclose_2026-09-10.log`)
* 시장 참고: KIS `kis_scan.py outcome 069500`·`229200`(2026-09-11 08:00~09:30)
* 집계 `analyze.py` · 배점 `sweep.py --pick` · 북극성 `sweep.py --walk` · 라벨 검증 `barcache.py` + `replay.py --verify`(944행, 불일치 0) · 반사실은 `sweep.load`/`sweep.sessions` 위 스크래치 계산
