# gapbet 예측 검증 — 2026-08-17(월) **휴장일**

## 0. 개요

| 항목 | 내용 |
|---|---|
| 검증일·시각 | 2026-08-17(월) 09:30 예약 실행 |
| **거래일 여부** | **휴장 — 광복절(8/15 토) 대체공휴일.** `kis_scan.py trail 005930 --date 20260817` → **bars 0**(가정 아님, 실측). 다음 거래일 **2026-08-18(화)** |
| 평가 리포트 | **없음** — 직전 거래일(2026-08-14) postclose 리포트가 생성되지 않았다 |
| 판정 표본 | **0건.** 오늘 09:00~09:30 자체가 없다 |
| 라벨-집행 일치 | `barcache.py prune`(362행 유지) → `replay.py --verify` **0/362 불일치** |
| 누적 데이터 | 362행 / 18회차 — **08-14 리뷰 이후 변동 없음** |

STEP 1 규정대로 **STEP 2~4는 표본이 없어 건너뛰고 STEP 5(누적 튜닝)만 수행**했다.

### 결함 2건

**결함 1 — 2026-08-14 종가 스캔 실패(운영).** `reports/logs/gapbet_2026-08-14.log` 전문이 한 줄이다:

```
API Error: 529 Overloaded. This is a server-side issue, usually temporary — try again in a moment.
```

`reports/run_gapbet.cmd`에 **재시도가 없다.** 1회 호출이 실패하면 그 회차는 그대로 소멸한다. 이번이 처음이 아니다 — postclose 스캔 17회 중 **2회(11.8%)가 리포트 없이 끝났다**(2026-08-05 `Ctrl+C`, 2026-08-14 API 529). 08-05 실패는 **2026-08-06 회차를 통째로 지웠고**(누적 dataset에 08-06이 없는 이유가 이것이다), 08-14 실패는 **08-18 회차의 A 모드 진입 근거를 지웠다.**

* 복리 단위로 환산하면: 회차 소실률 11.8% → 246회차 중 약 29회차 소실 → 현행 기하평균 +0.4524%에서 **1년 배수 3.04배 → 2.66배**. 대기열의 어떤 튜닝 레버보다 큰 폭이고, 데이터가 아니라 배관 문제다. → §4-7 제안 B-2

**결함 2 — 스케줄러가 휴장일을 모른다.** 오늘 09:30 리뷰가 돌았고(이 문서), 15:40 종가 스캔도 예정대로 돈다. 분봉이 0이라 스캔은 빈손으로 끝나지만 **로그·API 호출·커밋이 발생**하고, 리뷰는 「직전 거래일 리포트 없음」을 매 휴장일마다 결함으로 재보고하게 된다. 비용은 낮으나 결함 1의 재시도 로직이 휴장일에 재시도를 반복하면 비용이 커진다 — B-2 제안에 휴장일 가드를 같이 넣었다.

---

## 1. Top 후보 성적표

**해당 없음(휴장).** 판정할 회차가 존재하지 않는다.

## 2. 탈락 종목 검증

**해당 없음(휴장).**

---

## 3. 실행되지 않은 회차 분석 — 08-14 A 모드 재검증을 지금 소급했다

08-14에는 preclose(B 모드) 리포트만 남았다. B 모드는 gapbet.md상 **「장 마감 후 A 모드 재검증 전제로만 집행한다」**인데 그 재검증이 529로 실패했다. 재검증의 실체는 확정 수급 게이트(`both_buy_today`: 외국인·기관 **각각** ≥ +1.0억)이고, 이 값은 지금도 조회된다. 그래서 돌렸다.

| B 모드 순위 | 종목 | 코드 | 08-14 확정 외국인 | 08-14 확정 기관 | `both_buy_today` | A 모드 판정 |
|--:|---|---|--:|--:|:--:|---|
| 1 | 한미반도체 | 042700 | **−138.0억** | **−9.0억** | `False` | **`수급미달` 탈락**(both_sell) |
| 2 | LG전자 | 066570 | +388.1억 | +8.9억 | `True` | **통과** |
| 3 | SK하이닉스 | 000660 | +12,447.7억 | **−5,515.8억** | `False` | **`수급미달` 탈락**(foreign_only) |

* **재검증이 돌았다면 3종목 중 2종목이 게이트에서 탈락했다.** 즉 B 모드 리포트대로 08-14 종가에 진입한 물량이 있다면, 그중 2종목은 **게이트가 걸렀어야 할 종목을 3일 연휴 너머까지 들고 가는 상태**다.
* B 모드 리포트 자신이 이 위험을 정확히 지목해 뒀다 — 「특히 LG전자와 한미반도체가 취약하다」. 실제로는 한미반도체가 양측 순매도로 뒤집혔고, 리포트가 안전하다고 본 SK하이닉스가 기관 −5,516억으로 뒤집혔다. **잠정 가집계(외+기 합산 수량)가 기관 부호를 감춘다**는 B 모드의 구조적 한계가 3종목 중 2종목에서 발현한 것이고, 이건 A 모드 재검증이 존재하는 유일한 이유다.
* **이 3행은 dataset에 적재하지 않는다.** 집행 근거가 A 모드가 아니고(라벨=집행 일치 위반), 실제 진입 여부도 확인되지 않았다. 08-18 아침에 실제 보유분이 있으면 그때 판정한다.

---

## 4. 누적 집계 & 오늘의 제안

누적 **362행 / 18회차**(매수 55 · 게이트 탈락 183 · 순위 미달 124). **새 표본 0건 — 아래 수치는 전부 08-14 리뷰와 동일하다.** 값이 같다는 것은 재확인이 아니라 **같은 데이터를 다시 읽었다**는 뜻이므로, 어느 지표도 「연속 확인」으로 세지 않는다.

### 4-1. 북극성 — `pnl`

| 지표 | 값 |
|---|---|
| **회차복리(기하평균, 차감 후)** | **+0.4524%/회차 → 1년(246회차) 3.04배** |
| **깊은 손실 `deep_loss_rate`** | **5.5%**(3/55) · 깊은 갭하락 7.3% |
| `negative_expectancy` | **false** → 대원칙 8 미발동 |
| 청산 경로 | trail 26 · stop 19 · time 7 · weak_dn_close 3 |
| 회차 단위 | 손실 10/17회차 · 최악 −1.42% · 최고 +6.71%(08-14) |
| 건당 | 평균 +0.467% · 중앙값 +0.86% · 표준편차 2.81 · 최악 −4.28 · 최고 +8.18 |

* 진단 지표(조종간 아님): `separation_within_day` **0.000** · `mantel_haenszel_or` 0.825 · `day_variance_share` 0.184.
* `gap_down_big_recovery` 4.5%(n=22) < 손익분기 6.0% — 트레일 유지.

### 4-2. Type II 비용 (대원칙 9)

| 항목 | 값 |
|---|---|
| ⓐ 게이트가 놓친 실현 손익 합(`gate_wrong` 24/183) | **+106.23%p** (오탐률 13.1%) |
| ⓑ 트레일 조기 청산 포기분 평균 | **2.399%p** |
| ⓒ 게이트 부가가치 `selection_edge_pct` | **+0.459%p** |

사유별 초과수익(`by_reason`, `n_unbucketed` **0**): `inst_only` −0.14%p(n=79) · `foreign_only` +0.09%p(n=68) · `both_sell` +0.11%p(n=34) · 장후 공시 +2.08%p(n=7) · `non_flow` +0.72%p(n=2).

### 4-3. 게이트 양방향 성적표 (`gate_scorecard`)

| | n | 평균 | 음수 | 깊은손실(≤−3%) |
|---|--:|--:|--:|--:|
| 통과 | 179 | +0.514% | 42.5% | **4.5%**(`leak_rate`, 8건) |
| 탈락 | 183 | +0.008% | 49.2% | 6.6%(12건) |
| 차 | | | `neg_edge_pp` **+6.7%p** | `tail_edge_pp` **+2.1%p** |

회차 페어링 6승 3패. `tail_edge_pp`가 양수이므로 「게이트가 왼쪽 꼬리를 자른다」는 성립하지만 **폭이 얇고**, 게이트가 확실히 하는 일은 잔손실 빈도 축소 쪽이다. `leak_rows` 8건 중 5건이 `rank_scored`(미매수) — **배점의 공로로 쓰지 않는다.**

### 4-4. 배점 서열 진단 (비용 아님)

| 지표 | 값 | 읽기 |
|---|--:|---|
| `session_edge_t` | **+0.23**(n=14회차, edge +0.139%p) | \|t\| < 2 → **신호 없음(0과 구분 불가)** |
| `within_day_rank_tau` | +0.109(12회차 중 양수 7) | 서열이 정보를 못 만들고 있음 |
| `bought_only_tau` | +0.017(n=14) | 매수 3종목 내부 서열도 무신호 |
| `uplift_mean_pct` / `median` | −0.133 / −0.11%p | 안 산 종목이 평균적으로 회차를 낮춤(방향은 옳음) |
| 무작위 3종목 추출 | p05 −0.137 / 중앙 +0.368 / p95 +0.845 | 현행이 구간 안 |

### 4-5. 미결 항목 판정

새 표본이 없어 **전 항목 값 불변**이고, **마감일 경과 항목도 없다**(최근 마감 2026-09-30).

| 항목 | 조건 | 현재 | 판정 |
|---|---|---|---|
| `inst_only` 게이트 완화 | 갭상승률 통과군 대비 +10%p, n≥25 | +2.5%p(n=79) | 미달, 유지(2026-09-30) |
| `foreign_only` 게이트 완화 | +10%p, n≥25 AND 초과 ≥0 | +1.6%p(n=68) | 미달, 유지(2026-10-31) |
| 시총 하한 재설정 | 구간별 n≥10 | 1~5조 n=9 · 5조+ n=45 | 표본 미달(2026-10-31) |
| 갭하락 「상한없음」 vs 트레일 | rate vs 6.0%, n≥30 | 4.5%(n=22) | 미달, 트레일 유지(2026-09-30) |
| 채점 부가가치 ⓐ 게이트 | n≥20회차에서 +0.3%p 미만이면 재설계 | +0.459%p(18회차) | 미발동(2026-11-30) |
| 채점 부가가치 ⓑ 배점 | n≥20회차에서 t ≤ −2면 재설계 | t=+0.23 | 미발동(2026-11-30) |
| 게이트 꼬리 절단 `tail_edge_pp` | n≥25회차에서 +2%p 유지 | +2.1%p(18회차) | 미발동(2026-11-30) |
| 회차 스킵 탐색 종료 | 완벽 스킵 상한 대비 1.5배 미만 | 2.82배 | 계속(2026-12-31) |
| `sweep.py` 증분 캐싱 | `--pick` 3분 초과 | 17회차 10초대 | 미발동(2026-12-31) |

### 4-6. 반사실 발굴 (대원칙 10 — 오늘의 새 후보 2건, 둘 다 기각)

**후보 1: 「초기 손절선의 기준을 진입가에서 창 기준가(VWAP)로 옮긴다」.** 착안점은 깊은 손실 3건의 공통 구조다 — 아모레퍼시픽(−4.10)·LG전자(−3.53)·삼천당제약(−3.17)이 **전부 갭 −3% 부근으로 열려 진입가 −2% 손절선을 시초에 이미 관통한 상태**였고, 그래서 손절은 보호가 아니라 「열린 자리에서의 시장가 매도」로 변한다. 손절선이 갭과 함께 내려가면 그 관통이 사라진다. 기준가는 08:01에 확정되므로 누설 없음(판정 규칙 ⓐ 통과). 진입가 위로는 절대 올리지 않도록 `min(진입가−x%, 기준가−x%)`로 제한해 금지 계열(「이미 번 돈을 담보로」)을 피했다.

| 변형 | 회차복리(차감 후) | LOO최악 | 246배 | 최악회차 | **깊은손실** | 최악건 | 손익변경 |
|---|--:|--:|--:|--:|--:|--:|--:|
| **현행(진입가 −2%)** | **+0.4524%** | **+0.0959%** | **3.04** | −1.42 | **3/55** | −4.28 | — |
| 기준가 −1% | +0.4560% | +0.0998% | 3.06 | −2.27 | 7/55 | −4.51 | 21 |
| 기준가 −2% | +0.3772% | +0.0166% | 2.52 | −2.99 | 7/55 | −5.48 | 18 |
| 기준가 −3% | +0.2826% | −0.0832% | 2.00 | −3.72 | 11/55 | −6.44 | 19 |
| 진입가 −3%(대조) | +0.4166% | +0.0582% | 2.78 | −1.90 | 14/55 | −4.28 | 16 |

**기각 — 가설의 부호가 반대였다.** 손절선을 갭에 연동해 넓히면 깊은 손실이 **3건 → 7~11건으로 늘어난다.** 폭에 대해 단조이고(−1 → −2 → −3에서 복리·LOO·최악회차 전부 악화) 대조군(진입가 −3%)도 같은 방향이라 노이즈가 아니다. 읽기: **왼쪽 꼬리는 손절이 만드는 게 아니라 갭이 만든다.** 손절을 좁히면 긁히고 넓히면 더 깊이 물리므로, 진입가 −2%는 이미 꼬리 최소점 근처다. 기준가 −1%가 +0.0036%p 앞서지만 깊은 손실이 3→7건이라 1차 조종간 기준으로 열위다(그리고 절벽 바로 옆 값이다).

**따라서 깊은 손실을 줄이는 집행 레버는 소진됐다.** 남은 경로는 「종가 시점에 갭하락을 예측한다」뿐인데 그 능력은 `separation_within_day` **0.000**으로 측정돼 있다. 다음 회차부터 손절폭·손절기준 계열 제안은 이 표를 근거로 기각한다.

**후보 2: 「총점 절대 하한 재도입」 + 「후보 풀이 K개 미만인 회차 스킵」.** 상위 3종목 고정 규칙에는 스킵 경로가 없으므로(게이트 전멸 회차만 0종목), 절대 하한이나 풀 크기로 회차를 거를 수 있는지 봤다. 기준선은 **현행 규칙을 누적 전체에 소급 재선택**한 값이다(실현 기록 아님, 적용 배점 30/5/0/0/5/60 · 차감 전).

| 변형 | 회차복리 | LOO최악 | 회차 |
|---|--:|--:|--:|
| **현행(하한 없음)** | **+1.2263%** | **+0.9026%** | 18 |
| 총점 하한 30~60 | +1.2263% | +0.9026% | 18 |
| 총점 하한 65~70 | +1.2432% | +0.9205% | 18 |
| 총점 하한 75 | +0.9582% | +0.6197% | 17 |
| 총점 하한 80 | +0.7149% | +0.4037% | 13 |
| 풀 3개 미만 회차 스킵 | +1.2787% | +0.9580% | 16 |

**둘 다 기각.** ⓐ 총점 하한은 60까지 **완전히 무동작**(상위 3종목이 항상 그 위)이고, 65~70의 +0.017%p 이득은 노이즈 하한(0.10) 아래인 데다 **75에서 절벽**이다 — 「인접 값이 지지하지 않는 최적점 채택 금지」에 정확히 걸린다. ⓑ 풀 크기 스킵의 +0.052%p는 **풀이 1개·2개였던 07-21·07-24 두 회차를 뺀 결과**이고, 두 회차 다 초기 기록 누락(그때는 매수분만 채점했다)이라 실제 풀 크기가 아니다. 표본 2건에 fit이다.

### 4-7. 오늘의 제안

`pnl.negative_expectancy = false` → 대원칙 8 미발동.

* **A. 배점** — `sweep.py --pick --step 5`(17회차·159픽) 산출 = **`수급40 재료10 섹터5 종가0 미장15 과열30`**(LOO최악 +0.9530% · 전체 +1.2929% · 최악회차 −1.11% · 손실 5/17). gapbet.md 현재값(`수급30 재료5 섹터0 종가0 미장5 과열60`)과 다르므로 A 블록 유지.
  * **08-13·08-14와 동일한 벡터이지만 「3회차 연속 확인」이 아니다** — 새 회차가 하나도 안 들어왔으므로 같은 데이터를 세 번째 읽은 것이고, 독립 표본은 여전히 08-13·08-14 두 개다. A 블록 문구를 이에 맞게 정정했다.
  * 전 회차 대비 항목별 이동 0%p → 30%p 가드 미발동. `tuning_guard: BLOCKED`(분리도 4/6 동부호)는 **분리도로 배점을 옮기는 것**을 막는 플래그이고 배점은 대원칙 11의 LOO 최악값으로만 정한다.
* **B-1(유지, 08-14 제안)** — `analyze.py`에 `pnl.geo_mean_pct`·246회차 배수 출력 추가. 오늘도 §4-1 수치를 손계산했다.
* **B-2(신규)** — **`reports/run_gapbet.cmd`에 리포트 산출 확인 + 재시도 + 휴장일 가드.** 근거는 §0 결함 1: postclose 스캔 **17회 중 2회(11.8%)가 리포트 없이 종료**했고 각 실패가 회차 하나를 통째로 지웠다(08-05 → 08-06 소실, 08-14 → 08-18 근거 소실). 복리 환산 **3.04배 → 2.66배**로, 대기열의 어떤 모델 레버보다 크다. gapbet.md·`kis_scan.py`·배점은 건드리지 않는다.
* **모델 레버 변경 제안 0** — 게이트·집행·필터·국면 어느 쪽도 근거 게이트(confound·노이즈·표본·CI)를 통과하지 못했다. 반사실 2건은 위에서 기각, 미결 9건은 전부 조건 미달이며 마감 경과분 없음.
* **gapbet.md·`kis_scan.py`는 그대로다.** 위 제안은 전부 [`pending_changes.md`](../../.claude/skills/gapbet_review/pending_changes.md) 대기열에 있고 적용은 사용자 승인 시에만 이뤄진다.

### 4-8. 대기열 현황

* 미적용 제안 **3건** — A(배점, 최초 제안 2026-08-13 · 거래일 1일 경과) · B-1(측정 경로, 2026-08-14 · 0일) · B-2(운영 경로, 오늘 신규).
* **10거래일 초과 대기 항목 없음.**

### 4-9. 다음 거래일(2026-08-18) 인계

1. **08-14 B 모드 진입분이 실제로 있는지 확인.** 있다면 §3대로 **한미반도체·SK하이닉스는 A 게이트 탈락 종목**이다.
2. 08-18 아침 판정 대상은 **08-14 postclose 리포트가 아니라(존재하지 않음)** 실제 보유분이다. dataset 적재 여부는 집행 근거가 A 모드인지에 달려 있다 — 아니면 적재하지 않고 §0 결함으로만 남긴다.
3. **08-18 종가 스캔이 08-19 회차를 만든다.** 결함 1이 미해결이면 다음 529에서 같은 일이 반복된다.

---

## Sources

* 휴장 확인 = KIS `kis_scan.py trail 005930 --date 20260817`(bars 0), 조회 2026-08-17
* A 모드 소급 재검증 = KIS `kis_scan.py flow`(042700·066570·000660, 08-14 확정 수급)
* 실패 로그: `reports/logs/gapbet_2026-08-14.log` · 스캔 래퍼: `reports/run_gapbet.cmd`
* 누적 집계: `.claude/skills/gapbet_review/analyze.py` · 배점 재산출: `sweep.py --pick` · 반사실: `replay.py` + `barcache.jsonl`
