# gapbet 일일 리뷰 — 2026-09-18

## 0. 개요

* **검증일/시각**: 2026-09-18(금) 09:30 (자동 실행)
* **평가한 리포트**: **없음 — 2026-09-17 postclose 리포트가 생성되지 않았다.**
* **데이터 한계**: 판정 표본 0건. `dataset.jsonl`·`gate_failed.jsonl`은 어제(09-17 09:36) 이후 **1행도 추가되지 않았다.** 따라서 §4의 누적 수치 전량은 어제와 **정의상 동일**하다(재계산으로 확인).
* **모든 손익 수치는 왕복 거래비용 0.18%p 차감 후**다. 예외는 §2-2 열 이름에 명시(오늘은 해당 표 없음).

### 0-1. 결함 — 2026-09-17 두 모드 동시 실패(gapbet_review.md STEP 1)

직전 거래일 리포트가 없다. **STEP 2~4를 건너뛰고 STEP 5(누적 튜닝)는 정상 수행**했다.

| | 예약 시각 | 시작 | 종료 | 소요 | 반환코드 | 로그 |
|---|---|---|---|---|---|---|
| preclose | 14:45 | 14:45:00 | 14:49:18 | 4분 18초 | **0xC000013A** | **0바이트** |
| postclose | 15:40 | 15:40:00 | 15:41:06 | 1분 06초 | **0xC000013A** | 176바이트(`attempt 1`에서 끊김) |

* `0xC000013A` = `STATUS_CONTROL_C_EXIT`. **작업 시간 제한(PT3H)은 근처도 가지 않았고**, 한도 초과 메시지(09-10 실패의 특징)·응용 프로그램 오류 이벤트·로그오프 이벤트는 **없다.** 두 모드가 51분 간격으로 같은 코드로 죽었다 → 스킬·시장 데이터 층이 아니라 그 위(claude.exe 실행 경로)의 공통 원인이다.
* **재시도 가드(2026-08-21 신설)가 한 번도 발동하지 않았다.** `run_gapbet.cmd`의 로그는 자기 자신이 찍은 `[15:40:02.83] attempt 1`에서 끝난다 — 그 다음 줄인 `if exist REPORT` / `no report yet -- waiting 5 min`이 없다. **Ctrl+C가 콘솔 프로세스 그룹을 통째로 끝내므로 claude.exe와 함께 래퍼 cmd.exe도 죽는다.** 가드가 죽는 콘솔 안에 들어 있어 구조적으로 무력하다.
* **B 모드(preclose) 대체 경로도 같이 죽었다** — 5번의 슬롯 손실 중 3번째 동시 실패다(09-10 한도 공유, 09-17 이번 건). 두 모드가 같은 실행 경로·같은 계정을 쓰는 한 대체 경로는 독립적이지 않다.
* **오늘(09-18) 회차는 현금 슬롯으로 확정 소실**했다 — 누적 5번째(08-06 · 08-18 · 09-07 · 09-11 · 09-18). 08:00~09:30 창이 이미 지나 복구 경로가 없다.
* **부수 관측**: `claude.exe --dangerously-skip-permissions --resume`(PID 18376)이 **2026-09-16 14:05부터 이틀째 살아 있다**(CPU 700초, WS 280MB). 09-16에는 두 모드가 정상 완료했으므로 이 프로세스가 원인이라는 증거는 없다. 대화형 세션이라 이 리뷰에서 종료하지 않고 사용자에게 보고만 한다.

### 0-2. 결함 — `tuning_log.md` 파라미터 스냅샷 2줄이 실제 상태와 어긋난다

스냅샷은 **사용자 승인 후에만** 고칠 수 있으므로(STEP 6) 여기 결함으로만 적는다.

1. **북극성 기준일**: 스냅샷에 「배점을 마지막으로 바꾼 날은 **2026-08-12**, 이후 누적 **0회차**」로 적혀 있으나, 같은 파일 첫 줄의 배점은 「**2026-08-21 재적합 적용**」이고 `analyze.py`의 `RULES_APPLIED_DATE`도 **2026-08-21**(북극성 19회차)이다. 「0회차」는 08-12 시점에 쓰인 뒤 갱신되지 않은 값이다.
2. **폐기된 라벨 잔존**: 「리뷰 판정 규칙(2026-08-13 개정)」 줄이 순위 미달을 **`rank_miss`/`rank_ok`**로 적고 있으나 그 이진 판정은 **2026-08-14에 폐기**(현행 `rank_scored` + `rank_uplift_pct`)됐고, 같은 줄의 `type2_cost.rank_missed` 경로명도 현행 `rank_diagnostic`과 다르다.

둘 다 측정에는 영향이 없다(스크립트는 자기 상수를 읽는다). 다만 스냅샷은 「비교·판정의 기준선」으로 읽히는 문서라 다음 개편에서 죽은 값이 근거로 되살아날 수 있다.

## 1. Top 후보 성적표

**해당 없음** — 평가할 리포트가 없어 판정 표본 0건. 오늘 회차의 기하평균·깊은 손실 건수도 정의되지 않는다(매수 0종목, 현금 슬롯).

## 2. 탈락 종목 검증

**해당 없음**(2-1 게이트 탈락 / 2-2 순위 미달 모두 표본 0건).

## 3. 빗나간 예측 분석

예측이 없었으므로 예측 오차 분석도 없다. 대신 **잃은 것은 예측력이 아니라 복리 슬롯**이다.

* 누적 42슬롯 중 **5슬롯(11.9%)이 리포트 미생성으로 현금 처리**됐다. 슬롯 손실은 기대값이 양수인 전략에서 직접 복리를 깎는다 — 어제 회차(+0.347%)처럼 양수 회차가 그 자리에 들어올 수 있었다.
* 5회의 실패 원인이 **서로 다르다**: 08-06·08-18(초기), 09-04 리셋 18:00(재시도 창 밖), 09-10 한도 리셋 22:00(NXT 마감 뒤), **09-17 Ctrl+C(래퍼째 사망)**. 공통점은 **실패가 조용하다**는 것 하나뿐이다 — 다섯 번 모두 다음 아침 리뷰가 열릴 때까지 아무도 몰랐고, 그 시점엔 진입 창이 이미 닫혀 있었다.
* 따라서 유효 레버는 실패 유형별 대증 처방이 아니라 **ⓐ 가드를 죽는 콘솔 밖으로 빼기 + ⓑ 리포트 부재를 즉시 알리기**다. 09-17은 ⓐ 없이는 ⓑ만으로도, ⓑ 없이는 ⓐ만으로도 막히지 않았다.

## 4. 누적 집계 & 오늘의 제안

표본이 1행도 늘지 않았으므로 **아래 전 수치는 어제(09-17)와 동일하다.** `barcache.py prune`(1,071행, 제외 0) → `barcache.py build`(캐시 완비) → `replay.py --verify` **불일치 0/1,071**로 라벨=집행 일치를 확인했다.

* 표본: 채점 행 437 · 게이트 탈락 634 · Top 115 · 순위 미달 322 · 38회차(집행 37, 스킵 1)
* `pnl`: 건당 평균 **+0.053%** · 중앙 +0.05% · 양수율 51.3% · σ 2.61%p · 최악 −6.69% · 최고 +8.18% · **`negative_expectancy` false**
* **1차 조종간 — 깊은 손실**: `deep_loss_rate` **7.0%** · 깊은 갭하락 7.8%. 어제와 동일(신규 표본 없음)
* `exit_kind`: 트레일 47 · 손절 41 · 시간청산 21 · `weak_dn_close` 6
* `day_pnl`: 손실 회차 **21/38(55.3%)** · 최악 회차 −3.683%

### 4-1. 복리율 — 네 줄을 합치지 않는다

| 성적의 종류 | 출처 | 값 |
|---|---|---|
| **북극성 — 답을 안 보고 매 회차 다시 뽑아 산 결과** | `sweep.py --walk` | 표본 외 **27회차 · 회차당 +0.1390% · 누적 +3.82%** · 손실 14회차. 실현 평균 +0.1611%/회차 · σ 2.16%p · **t=+0.39 → 0과 구분 불가** |
| 현행 규칙으로 실제 산 것 | `pnl.north_star` | **19회차 · 회차당 −0.1417% · 누적 −2.659%**. 판정까지 21회차 부족 → **246회차 배수 환산 금지** |
| 지금 배점을 과거 전부에 대입(답을 보고 푼 값) | `sweep.py --pick` | 전체 **+0.3735%** · LOO 최악 **+0.2030%** · 최악회차 −2.72% · 손실 18/37 |
| 시작부터 지금까지 실제 산 것 전부(규칙 섞임) | `pnl.as_traded` | 슬롯당 +0.0308% · 누적 +1.302%(42슬롯). **돈이 새는지 보는 용도 — 근거 인용 금지** |

* **추세**: `--walk` 전반 13회차 실현 **+0.4528%/회차** vs 후반 14회차 **−0.1516%/회차**(차 +0.6219%p, Welch t=+0.73 → 표본 부족, 국면 변화와 구분 불가). 같은 기간 **표본내 적합값도 +0.7130% → +0.5369%로 같이 내려갔다**(처음 +0.6093 → 최고 +1.1129 → 마지막 +0.3742). 답을 보고 푼 값이 유지되는데 실현만 빠진 형태가 아니므로 **과적합 심화보다 6항목이 시장을 덜 맞추는 쪽**으로 읽힌다. 다만 부호 판정에는 표본이 못 미친다.
* **배점 궤적**: 직전 회차 대비 L1 이동 평균 **12.3**(전반 16.2 → 후반 8.5) · 최종 배점과의 거리 70 → 0 · **최종 배점 연속 유지 8회차**. 수익률은 아직 무의미하지만 배점은 멈춰 섰다 — 두 축을 한 문장으로 합치지 않는다.
* 북극성과 「현행 규칙으로 실제 산 것」의 **부호가 갈린다**(+0.1390% vs −0.1417%). 사유: 후자의 19회차는 `RULES_APPLIED_DATE` 2026-08-21 이후 구간이고, 여기엔 `--walk` 후반의 손실 회차(09-10·09-14·09-15)가 전부 들어가 있다. 전자는 그 앞 구간(전반 +0.4528%)을 포함한다.
* 각주: `--walk`가 재적합하는 것은 **배점뿐**이다. 게이트·집행 규칙 세대는 dataset 행에 굳어 있으므로 그쪽을 바꾼 회차가 섞여 있다.

### 4-2. Type II 비용(대원칙 9 — 비용은 이 둘뿐)

| 항목 | 값 |
|---|---|
| ⓐ 게이트가 놓친 실현 손익 합(`gate_wrong` n=54) | **+252.35%p** · 건당 베타조정 초과 **+4.32%p** |
| ⓑ 트레일 조기 청산 포기분(`peak_gain_pct` − `exit_pct`) | 건당 **2.169%p** |

사유별 베타조정 초과(`by_reason`, **`n_unbucketed` 0** — 결함 없음):

| 버킷 | n | 초과%p |
|---|--:|--:|
| 장후 공시 | 15 | **+1.74** |
| 양측 순매도/개인 주도 | 108 | +0.51 |
| 수급 외 사유 | 7 | +0.02 |
| 기관 단독 매수 | 229 | −0.10 |
| 외국인 단독 매수 | 289 | −0.11 |

### 4-3. 게이트 양방향 성적표(`gate_scorecard`)

| | n | 평균% | 음수 | 깊은손실(≤−3%) |
|---|--:|--:|--:|--:|
| 통과 | 437 | −0.025 | 50.3% | **5.5%**(24, `leak_rate`) |
| 탈락 | 634 | −0.373 | 57.7% | 7.4%(47) |
| | | | `neg_edge_pp` **+7.4%p** | `tail_edge_pp` **+1.9%p** |

* 회차 페어링 **14승 9패**. `tail_edge_pp` +1.9%p는 양수지만 미결 조건선(+2%p)에 미달 — 「게이트의 값은 왼쪽 꼬리 제거」는 아직 확정 문장이 아니다. 강화 발동선(0 근방·음수)도 아니다.
* `wrong_rate` 8.5%. `leak_rows` 24건 중 다수가 `rank_scored`(미매수) — **그 행들은 배점의 공로로 쓰지 않는다.**

### 4-4. 배점 서열 진단(비용 아님)

* `session_edge_t` **+0.75**(n=34, 평균 +0.21%p, 우위 19/34) · 통과 풀 τ **+0.039** · **`bought_only_tau` −0.053**(33회차) · 무작위 3종목 추출 중앙 −0.1851% [p05 −0.4756, p95 +0.1123]
* 판정: **「신호 없음(0과 구분 불가)」.** 적자라고 쓰지 않는다.
* 참고(`rank_diagnostic`): 미매수 322행 교체효과 평균 **−0.128%p** · 중앙 −0.09%p(음수 = 안 산 게 옳았다는 방향) · 최대 +10.37%p

### 4-5. 진단 지표(조종간 아님)

* `gap_dir`: `separation_within_day` **+0.016** · MH OR **1.00** · `day_variance_share` **0.23** · Top 갭상승률 39.1% vs 탈락 38.5%. **방향 선별력 0 — 결함 아님**(이 모델은 방향 예측기가 아니라 위험 축소기다)
* `factor_separation_within_day`(0.5=무신호): 재료 **0.657**(35쌍) · 섹터 0.542 · 미장 0.538 · 수급 0.500 · 과열 0.488 · 종가 0.479 / 총점 0.593 · 순위 0.537
* `tuning_guard`: **BLOCKED**(6항목 중 음수 4·양수 2 = 총점/국면 교란) → **항목별 분리도로 배점을 옮기지 않는다.** 배점은 `sweep.py --pick`의 LOO 최악값으로만 정한다(대원칙 11·4③)
* `gap_down_big_recovery`: 대박 **2/54 = 3.7% < 손익분기 6.0%** → **현행 트레일 유지**(2026-08-25 종결 판정 그대로)
* `by_market_cap`: 5조+ 승률 54.2%(96건) vs 1~5조 35.7%(14건) vs 3천억~1조 0%(2건). 대형 우위 방향은 유지되나 소형 표본이 여전히 얇다

### 4-6. 반사실 발굴(대원칙 10 — 새 후보 1개) — **기각**

**후보: 회차 최고 총점 하한 스킵.** 그 회차 1위 후보의 총점이 θ 미만이면 회차 전체를 현금(×1.0)으로 보내고 슬롯은 소모한다. 상위 3종목 선택은 그대로 둔다. 2026-08-10에 폐지된 절대컷은 **종목별** 컷이었고 이것은 **회차 단위 스킵**이라 한 번도 집행·측정된 적이 없다. 쓰는 값은 품질 점수와 적용 배점뿐이라 **종가 시점 관측 가능**(누설 없음).

| 배점 | θ | edge%p | LOO 최악 | 바뀌는 회차 |
|---|--:|--:|--:|--:|
| 적용(40/5/5/0/20/30) | 65 | −0.0459 | −0.0472 | 1 |
| 적용 | 68 | −0.0359 | −0.0729 | 4 |
| 적용 | 70 | −0.0384 | −0.0754 | 5 |
| 적용 | 72 | +0.0033 | **−0.1012** | 11 |
| 적용 | 75 | +0.0220 | **−0.0821** | 16 |
| A블록(15/25/40/0/0/20) | 60~62 | +0.0267 | +0.0084 | **2** |
| A블록 | 65 | +0.0221 | −0.0344 | 4 |
| A블록 | 68 | −0.1423 | −0.2034 | 9 |
| A블록 | 72 | −0.2746 | −0.3437 | 19 |

**기각 사유**: 적용 배점에서는 회차를 바꾸는 모든 θ의 **LOO 최악이 음수**다 — 양수 edge(θ72·75)는 37회차 중 11~16회차를 통째로 버려서 얻은 값이고 한 회차를 빼면 부호가 죽는다. A블록 배점의 θ60~62는 edge +0.0267·LOO +0.0084로 부호가 살지만 **바뀌는 회차가 2**뿐이고, θ≥68에서 부호가 크게 뒤집힌다. 격자 9개 중 최댓값을 2회차 표본에서 고른 것이라 **「표본에서 최적점을 찾아 채택」 금지 사항**에 정확히 해당한다. 미결 항목으로 등록하지 않는다(같은 후보를 매일 재계산하지 않기 위해 여기 기각 근거를 남긴다).

### 4-7. 미결 항목 판정 — **표본 0행 추가 → 전 항목 수치 어제와 동일, 마감 경과 0건**

`dataset.jsonl`·`gate_failed.jsonl`이 09-17 09:36 이후 변경되지 않았으므로 모든 반사실 수치는 어제 값과 **정의상 동일**하다. 가장 가까운 마감은 **2026-09-30**(기관 단독 매수 게이트 완화)으로 오늘 경과분 없다. 값 전량은 [review_2026-09-17](review_2026-09-17.md) §4-7과 `tuning_log.md` 미결표 참조. 요지:

* 조건 미달·부호 반대 유지: 기관 단독 완화(−0.10%p) · 외국인 단독 완화(−0.11%p) · 약세 스킵(edge −0.0433, LOO −0.1492) · 국면 문구 4종 스킵(전부 음수) · 종목별 갭 변동성 상한(θ 전 구간 음수) · 제3주체 종목 집중(+6.5%p < +20%p)
* n 요건만 부족: 장후공시 면제 확대(+1.74%p, n=15 → 20까지 5건) · 품질 하한 ④종가 θ0.2(+0.0813 · LOO +0.0404, 바뀌는 회차 2 → 10까지 8) · 애프터 프리미엄 θ0.0%(+0.0805 · LOO +0.0260, 바뀌는 회차 9 → 10까지 1) · 「강세·주도테마 지속」 스킵(+0.1249 · LOO +0.0759, n=4 → 10까지 6)
* 관찰 유지: 채점 부가가치 ⓐ +0.426%p·ⓑ t=+0.75 → 재설계 발동 안 함 · `tail_edge_pp` +1.9%p · 3주체 게이트(+1.5%p) · 제3주체 예외 존치(+6.9%p) · 회차 스킵 격차 4.20배(현행 1.079 vs 완벽 4.526) · `--pick` 실행시간 2분 이내
* **「A 모드 실패 회차에 B 모드 바스켓 보유」 — 갱신.** 오늘 새 실패 회차 1건(09-18)이 생겼으나 **발동 불가**: 09-17 preclose도 같은 `0xC000013A`로 죽어 B 바스켓이 없다. **n=2 불변.** 5회의 슬롯 손실 중 **3회가 두 모드 동시 실패**(08-18 · 09-10 한도 공유 · 09-17 Ctrl+C)라, 이 대체 경로는 실패 원인이 공통 실행 경로에 있을 때 독립적으로 작동하지 않는다. **조건에 「두 모드가 서로 다른 실행 경로를 쓸 때만 유효」를 명시**하고 마감(2026-11-30)까지 관찰 유지

### 4-8. 배점 — `sweep.py --pick` 기계적 산출

* 산출값 **`수급15 재료25 섹터40 종가0 미장0 과열20`** — **09-03 이후 매 회차 동일 벡터**, LOO 최악 +0.2030%로 37회차 1위. gapbet.md 현재값(`수급40 재료5 섹터5 종가0 미장20 과열30`) 계열은 7위(LOO +0.1703)
* 격차 **0.0327%p**, 2~5위도 전부 섹터 축 → sweep이 결정한 것은 **「섹터 축에서 ④종가·⑤미장이 죽었다」** 뿐이다(대원칙 11: argmax 자동 채택 금지)
* 전 회차 대비 **한 항목도 30%p 넘게 튀지 않았다**(변동 0)
* **표본 외 맞대결 누적 변동 없음** — 오늘 회차가 없어 새 판정이 생기지 않았다. **합 −0.010%p(판정 4 · 동률 4)** 유지: 09-04 −0.33 · 09-08 동률 · 09-09 +0.54 · 09-10 동률 · 09-14 −0.74 · 09-15 동률 · 09-16 +0.52 · 09-17 동률. **우열 없음**
* 「②재료 배점 부활」 미결 항목은 이 벡터와 같은 사안(②=25, 조건 10 이상 충족 11회차 연속). A 블록 승인이 곧 그 항목 채택

### 4-9. 오늘의 제안

`pnl.negative_expectancy`는 **false**이므로 대원칙 8의 강제 조항은 걸리지 않는다. 그럼에도 **제안 1건**을 올린다 — 오늘 잃은 것은 예측력이 아니라 복리 슬롯이고, 그 원인은 측정됐다.

* **A 블록(배점)**: `sweep.py --pick` 산출값으로 덮어썼다. 값은 어제와 동일하며 gapbet.md는 **그대로**다
* **B 블록 ⓛ 갱신(제안 1건, 레버 ⓘ 운영 경로 — gapbet.md·게이트·집행·배점 변경 0)**: 재시도 가드를 **콘솔 밖으로** 뺀다. 09-17은 가드가 래퍼 cmd.exe와 함께 죽어 **한 번도 발동하지 못했다**(§0-1) — 재시도 창을 늘리는 종전 안은 래퍼 생존을 전제하므로 이 실패를 막지 못한다. 처방을 **독립 예약 작업 인스턴스**(각 인스턴스가 리포트 존재를 먼저 확인하고 있으면 즉시 종료)로 교체하고, 마지막 인스턴스가 리포트 부재를 확인하면 알림한다
* **문서 정정 대기(승인 필요)**: §0-2의 스냅샷 2줄(북극성 기준일 08-12→08-21·누적 0회차→19회차 / 폐기된 `rank_miss`·`rank_missed` 잔존). 집행 변경 0의 문서 정리이므로 `pending_changes.md`에 올리지 않고 여기 결함으로만 남긴다
* **제안이 아닌 것**: 게이트·집행·배점 레버는 오늘 제안 0이다. 표본이 0행 늘었으므로 어제 근거에서 움직인 것이 없고, `tuning_guard`는 여전히 BLOCKED다
* **대기열 현황**: 미적용 제안 **2건**(A 배점 1 · B 운영 1). 가장 오래된 제안일 **2026-09-07**(ⓛ, 09-11·09-18 갱신) — 등록 후 9거래일로 **10거래일 만료 전**이며, 오늘 새 실측으로 근거를 강화·처방을 교체했다. 10거래일 넘긴 기각 대상 없음
* → **[pending_changes.md](../../.claude/skills/gapbet_review/pending_changes.md)** · gapbet.md는 아직 **아무것도 반영되지 않았다**

## Sources

* 판정: KIS `kis_scan.py trail`(분봉 경로 재현, 트레일링 청산) — **오늘은 판정 표본 0건**
* 집계: `analyze.py` · `sweep.py --pick` · `sweep.py --walk` · `barcache.py prune|build` · `replay.py --verify`(불일치 0/1,071)
* 결함 원인: 작업 스케줄러 Operational 로그(이벤트 102/201, 반환코드 3221225786 = 0xC000013A) · `reports/logs/gapbet_2026-09-17.log` · `reports/logs/gapbet_preclose_2026-09-17.log`(0바이트)
* 평가 대상 리포트: **없음**(`docs/gapbet/gapbet_report_2026-09-17.md` 미생성)
